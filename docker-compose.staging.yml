services:
  backend:
    environment:
      FRONTEND_BASE_URL: ${FRONTEND_BASE_URL:-https://nerotechnology.online}
      AUTH_COOKIE_SECURE: "true"
    ports:
      - "127.0.0.1:8000:8000"

  frontend:
    ports:
      - "127.0.0.1:3000:80"
